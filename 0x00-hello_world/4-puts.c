#include <stdio.h>
/**
 * main - A program thath prints a line with puts function
 * Return: 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingula puzzle);
return (0);
}
